# Supabase Plan

This folder contains draft migration material for CHOD AI OFFICE.

Do not apply migrations until the project owner approves the Supabase migration phase.

Current phase:

- Supabase client/server config wrappers are prepared.
- Runtime still works without Supabase.
- Mock mode and Google Sheet connector remain active.
- Approval permissions are still mock UI state.

Before applying any migration:

1. Review `docs/SUPABASE_SCHEMA_PLAN.md`.
2. Review `docs/RLS_POLICY_PLAN.md`.
3. Confirm zero-cost/free-tier limits.
4. Confirm Supabase project ownership.
5. Confirm environment variables.
6. Run migration only after explicit approval.
